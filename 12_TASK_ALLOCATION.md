# 12. 다중 로봇 탐사 할당 (거리 기준 ↔ LLM)

> 상위 문서: [Readme.md](Readme.md) · 앞 장: [10. 맵 병합](10_MAP_MERGE.md) ·
> 관련: [01. 인터페이스](01_INTERFACES.md), [11. 데이터 수집](11_DATA_COLLECTION.md)

전역 지도에서 탐사 후보(프론티어)를 뽑아 **어느 로봇이 어디로 갈지** 정하는 부분이다.
`strategy` 파라미터로 거리 기준 베이스라인과 LLM 을 갈아 끼운다.

## 목차
- [1. 왜 베이스라인을 먼저 만들었나](#1-왜-베이스라인을-먼저-만들었나)
- [2. 구조와 실행](#2-구조와-실행)
- [3. 관측 스키마](#3-관측-스키마)
- [4. 거리 기준 베이스라인](#4-거리-기준-베이스라인)
- [5. LLM 전략](#5-llm-전략)
- [6. 측정 결과](#6-측정-결과)
- [7. 해결한 함정](#7-해결한-함정)
- [8. 파일 맵](#8-파일-맵)
- [9. 증류 데이터](#9-증류-데이터)

---

## 1. 왜 베이스라인을 먼저 만들었나

단일 로봇 실험에서 배운 것이 있다. 후보를 `dist / sqrt(cells)` 로 정렬해 주고 LLM 에게
고르라고 했더니 **항상 1순위를 골랐고 근거도 그 정렬 기준을 그대로 말했다.** 순서를
섞어도 같은 후보를 골랐으니 순서에 끌린 것은 아니지만, 결국 **정렬 한 줄이 하는 일을
느리고 불안정하게 재현**한 셈이다. 그걸 증류하면 정렬 함수를 배운 작은 모델이 나온다 —
아무 의미가 없다.

그래서 거리 기준 최적 할당을 **먼저** 구현했다. LLM 이 이걸 못 이기면 증류할 것이 없다는
뜻이고, 이기면 그 차이가 곧 증류 대상이다.

### 무엇이 거리로 환원되지 않는가

로봇 2대가 후보 N개를 나눠 갖는 문제는, 비용이 거리뿐이면 최적해가 즉시 나온다.
LLM 이 기여할 여지가 없다. 기여하려면 거리에 담기지 않는 정보가 있어야 한다.

이 프로젝트에는 그게 실재한다 — **기체마다 잘하는 지형이 다르다.**

| | 보는 방식 | 강한 곳 |
|---|---|---|
| 드론 | 층별 매퍼가 순항 고도의 **수평 단면**을 그린다 | 트인 공간을 빠르게 훑는다. 그 높이에 없는 낮은 장애물은 못 본다 |
| UGV | 지상 라이다로 바닥 높이를 조밀하게 본다 | 좁고 복잡한 곳 |

그래서 관측에 `openness`(후보 주변이 얼마나 트였는가)를 넣는다. 거리와 독립인 축이고,
여기서 두 전략의 판단이 갈린다.

---

## 2. 구조와 실행

```
/map_merged ─┐
/{ns}/odom   ├─▶ 프론티어 검출 ─▶ **할당** ─▶ /{ns}/goal_pose
/{ns}/map   ─┘                      ▲
                                    └── strategy 가 여기를 갈아 끼운다
```

할당은 원리적으로 중앙집중이다 — 다른 로봇이 어디로 가는지 모르면 겹침을 막을 수 없다.
그래서 로봇 컨테이너가 아니라 한 곳에서 돈다.

**master 가 아니라 별도 컨테이너(`explorer`)** 인 이유: master 는 맵 병합·RViz·시계라는
관제 기반이고, 탐사 할당은 그 위에 얹히는 정책이라 실험 중 자주 바꿔 낀다. 같이 두면
전략 하나 바꾸려고 RViz 와 맵 병합까지 재시작해야 하고, 할당기가 죽으면 관제까지 흔들린다.

### 실행

`profiles: ["explore"]` 라서 기본 기동에는 안 뜬다 (손으로 목표를 주며 실험할 때
켜져 있으면 서로 덮어쓴다).

```bash
# 거리 기준 베이스라인
docker compose -f docker-configs/mac/docker-compose.yml --profile explore up -d

# LLM 전략 + 증류용 데이터 기록
EXPLORE_STRATEGY=llm EXPLORE_PERIOD=90.0 EXPLORE_DATASET=/tmp/alloc.jsonl \
  docker compose -f docker-configs/mac/docker-compose.yml --profile explore up -d explorer
```

| 환경변수 | 기본값 | 뜻 |
|---|---|---|
| `EXPLORE_STRATEGY` | `distance` | `distance` \| `llm` |
| `EXPLORE_PERIOD` | `30.0` | 배정 주기(초). llm 은 [6절](#6-측정-결과) 지연을 보고 정할 것 |
| `EXPLORE_MIN_SEP` | `15.0` | 배정된 후보끼리 최소 간격(m) |
| `EXPLORE_DATASET` | (없음) | 파일 하나에 쓸 때만. 비우면 `EXPLORE_DATASET_DIR` 에 실행마다 파일을 만든다 |
| `EXPLORE_BOUNDS` | (없음) | 탐사 범위 `x0,y0,x1,y1` (월드 좌표, m). 이 사각형 밖 프론티어는 후보에서 뺀다. 예: oneroom 건물 안 `-37.0,-37.0,37.0,37.0` |
| `EXPLORE_TAG` | (없음) | 실행 이름표. 증류 데이터 파일명과 meta 에 들어간다 ([9절](#9-증류-데이터)) |
| `EXPLORE_DATASET_DIR` | `/data/distill/raw` | 증류 원시 로그 폴더 (컨테이너 경로 — 호스트 `data/distill/raw`). `EXPLORE_DATASET` 을 주면 그 파일 하나에 쓰고 이 값은 무시된다 |
| `NVIDIA_API_KEY` | (없음) | `llm` 일 때 필요. compose 의 공통 anchor 가 호스트 값을 그대로 넘긴다 |

키가 없거나 응답이 스키마를 어기면 **매 주기 거리 베이스라인으로 물러난다.**
탐사가 멈추지는 않는다.

---

## 3. 관측 스키마

두 전략이 **같은 관측**을 본다. 이미지가 아니라 텍스트다 — 교사(큰 LLM)와 학생(증류될
작은 모델)이 같은 입력 분포를 써야 증류가 성립하기 때문이다.

```json
{
  "robots": [
    {"id": "ugv1", "type": "ugv", "x": -47.8, "y": -23.3,
     "sensor": "지상 라이다(Velodyne VLP-16) — 바닥 높이를 조밀하게 본다",
     "strength": "좁고 복잡한 실내",
     "nav_bounds": [-50.1, -49.8, -3.6, 49.3]},
    {"id": "drone1", "type": "drone", "x": -46.3, "y": 23.8, "...": "..."}
  ],
  "map": {"explored_pct": 17.6, "resolution": 0.1},
  "frontiers": [
    {"id": 1, "x": -49.6, "y": 41.0, "cells": 120, "openness": 0.667,
     "dist": {"ugv1": 64.4, "drone1": 17.4}}
  ]
}
```

- `cells` — 그 후보에 속한 경계 셀 수. 클수록 넓은 미탐색 영역
- `openness` — 후보 주변 반경 3 m 가 얼마나 트였는가 (0~1)
- `nav_bounds` — **그 로봇이 실제로 경로를 만들 수 있는 범위.** 드론은 층 매퍼가
  기체를 따라다니는 롤링 창만 갖고 있어서 그 밖의 목표는 Nav2 가
  `The goal sent to the planner is off the global costmap` 으로 무조건 실패한다(실측)

---

## 4. 거리 기준 베이스라인

거리 합을 최소화하는 **정확한** 할당이다. 로봇 k대·후보 n개면 조합이 `n!/(n-k)!` 뿐이라
(2대·후보 12개면 132가지) 완전탐색이 정확하고 즉시 끝난다. scipy 의 헝가리안을 쓸 수도
있지만 이미지에 우연히 깔려 있는 패키지라 의존을 만들지 않았다.

### 최소 분리를 일부러 넣었다

"서로 다른 후보" 만 보장하면 부족하다. 군집 버킷이 2 m 라 인접한 두 버킷이 별개 후보가
되는데, 그러면 **1.5 m 떨어진 사실상 같은 지점**에 두 로봇이 배정된다
(실측: ugv1 (-31.4, 38.4) / drone1 (-32.8, 38.9)). 분산 효과가 사라진다.

베이스라인에 일부러 넣었다 — LLM 이 이겼을 때 "거리 휴리스틱을 제대로 짰으면 될 일이었다"
는 반론을 막기 위해서다. 비교 기준은 공정해야 한다.

### 못 맞추면 단계적으로 낮춘다

예전에는 조건을 만족하는 조합이 없으면 제약을 통째로 버리고 "각자 가장 가까운 후보"로
무너졌다. 그러면 두 로봇이 같은 구역에서 부산하게 움직이며 1대만큼만 일한다.
지금은 `15 → 11.25 → 7.5 → 3.75 → 0` 으로 낮춰가며 **가능한 최대 분리**를 취한다.

`achieved_separation`(실제 확보한 값)과 `degraded`(요구치에 못 미쳤는지)를 반환에 담고,
20주기마다 집계를 찍는다.

---

## 5. LLM 전략

`assign_by_llm(obs, chat, ...)` 는 `chat: prompt -> 응답문자열` 콜러블을 주입받는다.
이 이음매 덕분에 할당 모듈이 ROS 와 openai 양쪽에서 떨어져 있고, 가짜 `chat` 으로
단위 검증이 된다.

OpenAI 호환이면 무엇이든 붙는다 — NVIDIA NIM, 로컬 vLLM, Ollama. `base_url` 과
`model` 만 바꾸면 코드는 그대로다.

### 검증하고 쓴다

모델을 믿되 검증한다. 실측에서 스키마 위반이 드물지 않았다.

1. `validate()` — 형식, 모르는 id, 중복 배정
2. `unreachable_in()` — `nav_bounds` 밖 배정. 스키마는 맞아도 그 주기가 통째로 낭비된다
3. 다 실패하면 **거리 베이스라인으로 폴백** (탐사가 멈추는 것보다 낫다)

폴백 횟수 자체가 지표다 — 교사가 얼마나 자주 쓸 수 없는 답을 내는지가 증류의 난이도다.

실제로 본 실패 유형:

| 응답 | 잡는 곳 |
|---|---|
| 본문이 빈 문자열 (`finish_reason='length'`) | `_chat()` 이 잘림으로 구분해 한 번 재시도 |
| **추론 텍스트가 본문 자리에 든 채** 잘림 (`finish_reason='length'`) | 같은 곳 — 본문 유무가 아니라 `finish_reason` 을 먼저 봐야 잡힌다 |
| `{}` | `assignments 가 비었다` |
| `{"": ""}` | `assignments 가 비었다` |
| 목록에 없는 후보 id | `validate()` |
| `nav_bounds` 밖 후보 | `unreachable_in()` |

> 📏 **실측 (2026-09-16, oneroom 내부 정찰 6회):** LLM 주기 96회 중 폴백 19회(20%).
> 잘림 재시도를 넣기 전 전체 지도 실험에서는 15주기 중 7회(47%)였다. 호출 원문을 남기기
> 시작한 뒤 4회만 보면 **HTTP 호출 175회 중 113회(65%)가 잘렸다** — 재시도가 폴백을 줄였을 뿐
> 잘림 자체는 흔하다. 잘림은 탐사 후반만의 일이 아니라 첫 호출부터 날 수 있다.

### 매 주기 베이스라인도 같이 계산한다

`strategy=llm` 일 때도 베이스라인을 계산해 **한 줄에 짝지어** 기록한다. 완전탐색이
132가지뿐이라 비용이 없고, 둘이 같은 답을 내는 주기는 학습 신호가 없다는 뜻이며,
갈리는 주기가 곧 증류 대상이다.

```json
{"observation": {...}, "result": {"method": "llm", ...}, "baseline": {...}, "stats": {...}}
```

---

## 6. 측정 결과

2대(ugv1 + drone1), 96×96 m 원룸, `min_separation=15.0`.

### 판단 품질 — LLM 이 낫다

15주기 중 LLM 이 유효한 답을 낸 8주기를 베이스라인과 짝지어 비교했다.

| | 값 |
|---|---|
| 베이스라인과 **같은** 배정 | 1회 |
| **다른** 배정 | 7회 |
| 다를 때 분리 차이 | **평균 +4.8 m** (최소 +0.1, 최대 +15.2) |
| 다를 때 거리합 차이 | 평균 +16.7 (최소 +4.1, 최대 +60.8) |
| LLM 이 분리에서 이긴 횟수 | **7/7** |

방향이 7/7 로 일관된다. LLM 은 **거리를 더 쓰더라도 두 대를 더 멀리 떼어 놓고**,
근거에서 드론을 `openness` 높은 후보로 보낸다고 말한다 — 거리에 담기지 않는 축이다.

### 온라인 탐사 성능 — 지연 때문에 진다

| | 커버리지 | 주기 |
|---|---|---|
| 거리 베이스라인 | **33.3%** @ 840s | 30초 |
| LLM | 17.5% @ 1080s | 60초 설정 → 실제 74~123초 |

**판단이 나빠서가 아니라 느려서 진다.** 호출 하나가 주기를 넘기니 로봇이 받는 목표 수
자체가 베이스라인의 1/3 이 된다. 폴백률도 47%(15주기 중 7회)였다.

> 🚨 **그래서 벽시계 기준 커버리지 비교는 LLM 전략의 판단 품질을 재지 못한다.**
> 이 표는 "지금 이 교사를 그대로 온라인 계획기로 쓰면 안 된다" 는 뜻이지
> "LLM 할당이 나쁘다" 는 뜻이 아니다. 오히려 **증류를 해야 하는 이유**다 —
> 같은 판단을 빠르게 내는 작은 모델이 목표다. 비교는 **주기 수를 맞춰서**
> 하거나, 교사를 오프라인으로 돌려 판단만 비교해야 한다.

### 응답 안정성 — 예산이 아니라 분산이 지배한다

같은 실제 관측(후보 12개)으로 3회씩:

| 후보 수 | `max_tokens` | 성공 | 평균 지연 |
|---|---|---|---|
| 12 | **2048** | **3/3** | **17.6초** |
| 12 | 4096 | 3/3 | 24.5초 |
| 12 | 6144 | 2/3 | 83.1초 |
| 8 | 4096 | 2/3 | 49.9초 |
| 6 | 4096 | 1/3 | 50.9초 |

예산을 **키워도 잘림(`length`)이 난다.** `detailed thinking off` 가 추론을 줄일 뿐
없애지 못해서 추론 길이가 실행마다 크게 흔들리고, 예산이 크면 더 길게 헤매다 같은 벽에
부딪힌다. 2048 아래는 확실히 부족하지만(768 → 5/5 전부 빈 응답), 그 위로는 예산이 아니라
분산 문제다. 그래서 기본값은 2048 이고, 잘렸을 때만 한 번 두 배로 다시 묻는다.

### 내부 정찰 비교 — oneroom 건물 안 (UGV + 드론)

위 측정은 100×100 m 지도 전체가 대상이라 마당·울타리까지 섞였다. 여기서는 **건물 내부만**
정찰하게 해서 두 전략을 다시 비교한다.

**지형.** `oneroom` 은 100×100 m 땅(울타리 ±49.75) 가운데 76×76 m 건물이 있고,
벽(두께 0.5 m) 안쪽이 **x, y ∈ [−37.5, 37.5] = 75×75 m = 5625 m²** 다. 칸막이·장애물 없음.
출입구는 남쪽 (0, −37.75) 과 서쪽 (−37.75, −15.5), 폭 3 m.

**조건** — 두 전략에 똑같이 준다.

| 항목 | 값 | 이유 |
|---|---|---|
| 편대 | ugv1 (−34, −18), drone1 (−34, −13) | 서쪽 출입구로 막 들어온 상황. 가운데서 시작하면 어느 전략이든 쉽게 끝난다 |
| 탐사 범위 | `EXPLORE_BOUNDS=-37.0,-37.0,37.0,37.0` | 벽 안쪽 면에서 0.5 m 들인다. 벽에 붙은 셀은 프론티어로 남아도 갈 수 없다 |
| 주기 | 60초 (두 전략 동일) | LLM 호출 지연을 감안한 값. 넘기면 `late` 로 센다 |
| 최소 분리 | 15 m | |
| 순서 | 베이스라인 → LLM → 베이스라인 → LLM | 한 전략을 몰아서 돌리면 Webots·API 상태의 시간 변화가 전략 차이로 섞인다 |
| 리셋 | 시행마다 `/remove_robot {all, force}` 후 `down`/`up` | [03장 8절](03_SPAWNER.md#-반복-실험에서-compose-down-은-리셋이-아니다) |

**지표.**

- **내부 커버리지** — 병합 지도에서 내부 75×75 m 창 안의 탐색된 셀 비율. 시뮬 시각 기준으로
  25 / 50 / 75 / 90% 도달 시각을 잰다. 🚨 **기준점은 10% 를 처음 넘은 순간**이다.
  측정기의 0초는 "지도와 시계를 처음 받은 시각" 이라 시행마다 다르고(실측: 한 시행은 6.55%,
  다른 시행은 1.22% 에서 시작), 0초 기준으로 재면 일찍 시작한 시행이 불리하다.
  같은 베이스라인 시행이 0초 기준 90% 480초 → 10% 기준 366초로, 114초가 시작점 차이였다.
  스텝(60초) 사이는 선형 보간한다
- **새 목표 생성 수** (`new_targets`) — 로봇에게 **새로** 내준 목표 수. 같은 목표를 주기마다
  다시 보내는 것은 세지 않는다. "탐사 점을 얼마나 덜 만들고 끝내는가" 가 이 값이다
- **목표 포기 수** (`giveups`), **이동거리** (UGV·드론 따로, odom 적분)
- LLM 전용: 평균 호출 지연, 주기를 넘긴 횟수(`late`), 폴백 수, 베이스라인과 판단이 갈린 주기

**종료 조건** — 두 전략에 똑같이.

빈 방은 10분 남짓이면 다 보인다(실측: 베이스라인이 660초에 99.87%, 이후 2000초까지 변화
없음). 고정 시간으로 재면 나머지를 헛돌리므로 다음 중 하나면 끝낸다.

1. 내부 커버리지 99.5% 이상
2. 95% 이상에서 5분 동안 0.05%p 도 안 늘면 — 벽 속 셀처럼 원래 못 보는 잔여

95% 조건을 거는 이유는 느린 전략이 중간에 잠깐 정체한 것을 완료로 오인하지 않기 위해서다.
완료 후에는 새 목표도 이동거리도 늘지 않으므로 이 조건이 지표를 바꾸지 않는다.

**결과** — 2026-09-16, 베이스라인 6회 · LLM 6회 (두 묶음: 2쌍 + 4쌍). 교사는
`nvidia/nemotron-3-super-120b-a12b`, 시스템 프롬프트 `detailed thinking off`, 예산 2048 (잘리면 4096).

| 지표 (평균 ± 표준편차) | 베이스라인 | LLM |
|---|---|---|
| 10% → 25% | **135 ± 28 초** | 283 ± 238 초 |
| 10% → 50% | **277 ± 52 초** | 537 ± 364 초 |
| 10% → 75% | **358 ± 60 초** | 801 ± 315 초 |
| 10% → 90% | **503 ± 215 초** | 974 ± 384 초 |
| 새 목표 생성 | 21.2 ± 5.2 개 | **17.2 ± 2.5 개** |
| 새 목표 / 커버리지 1% | 0.21 | **0.17** (−18%) |
| UGV 이동 / 커버리지 1% | **1.38 m** | 2.11 m (+53%) |
| 드론 이동 / 커버리지 1% | 1.20 m | 1.30 m |
| 목표 포기 | 0.2 회 | 0 회 |

시행별:

| 시행 | 10→50% | 10→90% | 새 목표 | 새 목표/1% | UGV m/1% | 드론 m/1% | LLM 호출 평균 | 잘린 호출 | 폴백 |
|---|---|---|---|---|---|---|---|---|---|
| 묶음1 베이스라인 1 | 222 | 366 | 18 | 0.18 | 1.31 | 1.17 | | | |
| 묶음1 베이스라인 2 | 332 | 776 | 28 | 0.28 | 1.95 | 1.39 | | | |
| 묶음2 베이스라인 1 | 264 | 379 | 21 | 0.21 | 1.38 | 1.36 | | | |
| 묶음2 베이스라인 2 | 282 | 388 | 20 | 0.20 | 0.96 | 1.10 | | | |
| 묶음2 베이스라인 3 | 340 | 781 | 26 | 0.26 | 1.68 | 1.41 | | | |
| 묶음2 베이스라인 4 | 221 | 325 | 14 | 0.14 | 0.99 | 0.78 | | | |
| 묶음1 LLM 1 | 320 | 951 | 19 | 0.19 | 2.18 | 1.35 | 91.9 초 | (기록 전) | 4 |
| 묶음1 LLM 2 | 966 | 1225¹ | 15 | 0.15 | 2.25 | 1.27 | 97.1 초 | (기록 전) | 6 |
| 묶음2 LLM 1 | 214 | 899 | 16 | 0.16 | 1.82 | 1.07 | 72.2 초 | 18/32 | 1 |
| 묶음2 LLM 2 | 985 | 1525 | 19 | 0.19 | 2.35 | 1.10 | 95.6 초 | 45/61 | 3 |
| 묶음2 LLM 3 | 180 | 381 | 14 | 0.14 | 1.69 | 1.01 | 70.8 초 | 20/32 | 2 |
| 묶음2 LLM 4 | 558 | 865 | 20 | 0.20 | 2.34 | 2.00 | 57.7 초 | 30/50 | 3 |

¹ 2000초 상한에 걸려 98.9% 에서 끝났다. 90% 는 그 전에 넘었다.

**교사의 판단.** LLM 이 유효한 답을 낸 77주기 중 **62주기(81%) 가 베이스라인과 다른 배정**이었고,
그중 41주기(66%) 에서 로봇 사이 분리가 더 컸다. 근거로는 거의 매번 "드론을 openness 가 높은
후보로" 를 든다.

**읽는 법.**

1. **빠르기는 베이스라인이 모든 구간에서 앞선다.** 90% 도달이 평균 503초 대 974초이고, LLM 쪽은
   편차가 크다 (381 ~ 1525초).
2. **느린 주된 원인은 판단이 아니라 지연으로 보인다.** 주기 60초에 호출 평균 58~97초라 주기 초과가
   43번 났고, 그동안 로봇은 목표에 도착한 뒤 서 있었다. 원문을 남긴 4회에서 잘림률↔90% 도달
   r = +0.67, 호출 지연↔90% 도달 r = +0.71 이다. **다만 n = 4 라 증거로는 약하다.** 지연이 짧았던
   묶음2 LLM 3 은 90% 를 381초에 넘어 베이스라인과 같았고, 묶음2 LLM 4 는 지연이 가장 짧았는데도
   865초가 걸려 지연만으로 다 설명되지는 않는다.
3. **탐사 점은 LLM 이 덜 만든다** (커버리지 1% 당 0.17 대 0.21). 대신 목표가 멀어서 UGV 가 53% 더
   달린다. 분리를 크게 잡는 판단 경향과 맞고, 긴 주기 동안 로봇이 지난 목표를 끝까지 끌고 가는
   효과도 섞여 있다 — 둘을 이 실험으로는 가를 수 없다.
4. **판단 간격당 탐색 증가** (내부 90% 전까지, m²/분) 는 평균 베이스라인 약 560 · LLM 약 630,
   중앙값 약 470 · 170 이다. LLM 쪽은 몇 번 크게 늘고 대부분은 적게 늘어 분포가 치우쳤다.
   간격 길이가 전략마다 달라 결론을 내리지 않는다.

**무엇을 바꾸면 판단 품질을 따로 잴 수 있나.** 지연을 없애야 한다 — (a) 교사가 생각하는 동안
시뮬을 멈추거나, (b) 같은 관측에서 두 전략의 배정을 각각 실행해 보는 반사실 실험, (c) 증류한 작은
모델처럼 지연이 짧은 할당기로 같은 판단을 재현하는 것.

**증류 데이터 수확.** LLM 6회에서 교사 유효 샘플 77개 (실행당 약 13개), 베이스라인과 갈린 사례
62건. 묶음2 의 4회는 프롬프트 원문·추론 텍스트·잘린 호출까지 남아 있고, 묶음1 의 2회는 기록 형식이
바뀌기 전이라 프롬프트를 관측으로 다시 만들었다 (`prompt_rebuilt`). 내보낸 결과는 train 40 · val 37 —
실행 단위로 나누는데 LLM 실행이 6개뿐이라 val 비중이 48% 로 커졌다. 실행이 쌓이면 설정한 20% 에
가까워진다.

---

## 7. 해결한 함정

### ① 경유점이 로봇 자기 위치로 뭉개져 영구 교착

커버리지가 20% 언저리에서 평평해지던 원인. 자세한 재현과 수치는
[10장 ⑬](10_MAP_MERGE.md#-탐사-커버리지가-20-언저리에서-평평해진다--경유점이-제자리로-뭉개진-것).

### ② 추론 모델은 예산을 추론에 먼저 쓴다

`max_tokens=768` 로 물어보면 **본문이 빈 문자열**로 오고 `finish_reason='length'` 가
된다. 호출부에는 `JSONDecodeError: Expecting value: line 1 column 1` 로만 보여서
"모델이 이상한 답을 한다" 로 오독하기 쉽다. 실측: 768 → 5/5 빈 응답, 3000 → `'{}'`.

잘렸다고 본문이 늘 비어서 오는 것도 아니다. `max_tokens=256` 으로 물었더니
`finish_reason='length'` 인데 본문에 `'Okay, the user is asking...'` 같은 **추론 텍스트**가
들어 있었다. 본문 유무로 판단하면 이걸 정상 응답으로 넘겨 JSON 파싱에서 죽는다.

`_chat()` 은 **`finish_reason` 을 본문보다 먼저** 보고, 잘렸으면 한 번은 예산을 두 배로
늘려 다시 묻는다.

### ③ Nemotron 은 추론 토글로 3배 빨라진다

시스템 메시지 `detailed thinking off` 를 주면 같은 답을 **29초 → 9.3초**로 낸다.
`/no_think` 는 이 모델에서 통하지 않았다(잘림). 다른 제공자면 `system_prompt` 를
빈 문자열로 두면 된다.

### ④ YAML 접힘 스칼라(`>`)는 더 깊게 들여쓴 줄의 줄바꿈을 보존한다

compose 의 `command: >` 안에서 launch 인자를 줄바꿈해 적으면 안 된다.

```yaml
# 🚫 이렇게 쓰면 bash 가 ros2 launch 를 인자 없이 실행하고
#    strategy:=llm 을 별도 명령으로 취급한다 — 에러가 안 난다
command: >
  bash -c "... ros2 launch webots_goal_bridge allocator.launch.py
             strategy:=llm
             period:=60.0"
```

증상은 조용하다. 노드가 **기본값(distance / 30초)으로 그냥 뜬다.** 확인 방법:

```bash
docker inspect explorer_mac --format '{{index .Config.Cmd 2}}' | wc -l   # 1~2 여야 한다
docker logs explorer_mac | grep "할당기 시작"                              # 전략이 찍힌다
```

### ⑤ LLM 호출이 타이머를 막으면 구독까지 멈춘다

rclpy 의 기본 실행기는 단일 스레드이고, 구독과 타이머가 같은 콜백 그룹이면 직렬화된다.
한 번에 12~123초 걸리는 호출이 타이머 안에 있으면 그동안 `odom`/`map` 구독이 통째로
멈춰, 다음 주기가 수십 초 묵은 위치로 배정하게 된다.

주기 처리를 별도 `MutuallyExclusiveCallbackGroup` 에 두고 `MultiThreadedExecutor`
(스레드 2개)로 돌린다. **한쪽만 바꾸면 효과가 없다.**

### ⑥ 반복 실험에서 `compose down` 은 월드 리셋이 아니다

macOS 는 Webots 가 호스트에서 네이티브로 돈다 →
[03장 8절](03_SPAWNER.md#-반복-실험에서-compose-down-은-리셋이-아니다).

### ⑦ 값이 빈 launch 인자(`name:=`)는 거부된다

compose 명령에 `dataset_path:=${EXPLORE_DATASET:-}` 처럼 **빈 기본값**을 두면, 변수를 안 준
기동에서 인자가 `dataset_path:=` 가 된다. `ros2 launch` 는 이것을

```
malformed launch argument 'dataset_path:=', expected format '<name>:=<value>'
```

로 거부하고, `restart: unless-stopped` 라 컨테이너가 **재시작을 반복**한다. 할당기가 없으니
로봇은 목표 없이 서 있고, 커버리지 측정은 그대로 돌아 시행 하나를 통째로 버린다(실측).

변수를 늘 채워서 돌린 실험에서는 드러나지 않았다가, 기본값으로 돌리는 순간 터졌다.
배포 전 검증도 못 잡았다 — 런치 파일의 **파라미터 평가**(`LaunchContext`)만 시험했고
**CLI 인자 파싱**은 거치지 않았기 때문이다. 이미지 검증은 compose 가 만드는 명령줄 그대로
`ros2 launch` 를 실행해 봐야 한다.

선택 인자의 compose 기본값은 빈 값 대신 `none` 이고, 할당기의 `opt_str()` 가
`''` / `none` / `null` 을 "안 줌" 으로 읽는다.

---

## 8. 파일 맵

```
src/webots_goal_bridge/
├── webots_goal_bridge/
│   ├── frontier_allocator.py       # 순수 모듈 (ROS 없음)
│   │                               #   build_observation / openness / reachable
│   │                               #   assign_by_distance  ← 베이스라인
│   │                               #   assign_by_llm       ← 교사
│   │                               #   validate / unreachable_in / assignment_cost
│   ├── frontier_allocator_node.py  # ROS 노드 (구독·배정 주기·경유점·집계·기록)
│   └── llm_goal_assigner.py        # 단일 로봇용 (extract_frontiers 를 여기서 가져온다)
├── launch/
│   └── allocator.launch.py         # explorer 컨테이너용
└── scripts/                        # 실험·증류 도구 (설치되지 않는다 — 저장소에서 바로 실행)
    ├── run_explore_compare.sh      # 전략 비교 반복 실행 (호스트 bash)
    ├── coverage_logger.py          # 탐사 범위 커버리지 기록 (master 컨테이너 안에서)
    ├── analyze_explore_compare.py  # 비교 분석
    ├── export_distill.py           # 원시 로그 → 증류 데이터셋
    └── experiments/
        └── oneroom_in2.override.yml   # 건물 내부 2대 편대

data/distill/                       # 증류 데이터 (git 에 안 올라감, 9절)

docker-configs/{mac,ubuntu,windows}/docker-compose.yml   # explorer 서비스
```

`frontier_allocator.py` 가 ROS 에 의존하지 않는 것은 의도다 — 격자와 숫자만 다루므로
시뮬을 띄우지 않고 검증할 수 있고, 학습 데이터를 오프라인으로 다시 만들 때도 그대로 쓴다.

### 관련 문서

- [10. 다중 로봇 맵 병합](10_MAP_MERGE.md) — `/map_merged` 를 만드는 쪽
- [01. 인터페이스 총람](01_INTERFACES.md#3-전역관제-인터페이스) — `/{ns}/goal_pose` 규격
- [03. 로봇 소환](03_SPAWNER.md) — 편대 구성과 반복 실험 리셋
- [11. 데이터 수집 (KITTI)](11_DATA_COLLECTION.md) — 카메라-라이다 쪽 데이터셋

---

## 9. 증류 데이터

할당기가 매 주기 내린 판단을 **학생 모델이 그대로 배울 수 있는 형태**로 호스트에 쌓는다.
explorer 컨테이너가 저장소의 `data/distill/` 를 `/data/distill` 로 마운트하고, 기본으로
`raw/` 에 **실행마다 파일 하나**(`{run_id}.jsonl`)를 만든다. 내용물은 git 에 올라가지 않는다.

```
data/distill/
├── raw/{run_id}.jsonl          # 원시 로그 — 지우지 말 것 (다시 얻으려면 실험을 다시 돌려야 한다)
│   └── legacy/                 # 기록 형식이 바뀌기 전 로그 (프롬프트·교사 원문 없음)
├── cov/{run_tag}.json          # 비교 실험의 커버리지 곡선
├── logs/{run_tag}_explorer.log
└── export/                     # export_distill.py 결과 — 언제든 다시 만든다
```

### 왜 실행마다 파일 하나인가

연속한 주기끼리는 관측이 거의 같다. 행 단위로 train/val 을 섞으면 검증 점수가 새므로
**실행 단위**로 나눠야 하고, 그러려면 실행 경계가 파일에 남아 있어야 한다.

### 한 줄에 무엇이 남나

첫 줄은 `{"type": "meta"}` — 모델·시스템 프롬프트·예산·주기·분리·탐사 범위·프롬프트 판.
섞어 학습할 때 조건이 다른 실행을 거르는 근거다.

이후 주기마다 `{"type": "round"}`:

| 필드 | 내용 | 증류에서 쓰는 곳 |
|---|---|---|
| `prompt` | 교사에게 **실제로 보낸** 사용자 프롬프트 원문 | 학생 입력. 기록과 호출이 같은 함수(`build_alloc_prompt`)를 쓴다 |
| `prompt_version`, `prompt_sha` | 프롬프트 판 | 판이 섞인 데이터를 가른다 |
| `observation` | 구조화된 관측 | 프롬프트를 다른 형식으로 다시 만들 때 |
| `result` | 실행된 배정 (llm 이면 교사 답, 폴백이면 베이스라인) | 학생 정답 |
| `baseline` | 같은 관측에 대한 거리 베이스라인 답 | 교사와 갈린 주기 찾기 |
| `teacher.calls[]` | HTTP 호출마다 본문·**추론 텍스트**·`finish_reason`·토큰·지연 — **잘린 호출도** | 추론 증류, 어려운 입력 분석 |
| `teacher.attempts[]` | 파싱·검증 시도마다 원문과 성패·사유 | 교사가 무너지는 입력 찾기 |
| `explored` | 판단 **시점**의 탐사 범위 내 탐색 셀 수 | 다음 행과의 차이 = 이 판단의 결과 |
| `sent`, `events` | 로봇별로 보낸 목표·경유점, 그 주기의 포기 | 결과 해석 |

배정이 없는 주기는 `{"type": "event", "event": "no_frontiers"}` 로 남는다 (탐사 완료 시각).

> 🚨 **프롬프트 문구를 바꾸면 `frontier_allocator.PROMPT_VERSION` 을 올린다.** 판이 섞이면
> 학생이 보는 입력 분포가 실행마다 달라진다. `PROMPT_SHA` 는 올리는 걸 잊었을 때의 안전망이다.

### 데이터셋으로 내보내기

```bash
python3 src/webots_goal_bridge/scripts/export_distill.py                       # raw → export
python3 src/webots_goal_bridge/scripts/export_distill.py --include-reasoning   # 추론까지 가르칠 때
python3 src/webots_goal_bridge/scripts/export_distill.py --min-gain 300        # 결과가 좋았던 판단만
```

| 출력 | 내용 |
|---|---|
| `sft_train.jsonl` / `sft_val.jsonl` | `{"messages": [user, assistant], "meta": {...}}` — 교사의 **유효한** 답만. 실행 단위 분할(run_id 해시라 다시 돌려도 같다) |
| `disagreements.jsonl` | 교사와 베이스라인이 다르게 고른 주기 — 두 답과 **실행된 쪽(교사)** 의 결과 |
| `summary.json` | 실행별 유효·폴백·갈림·지연 |

설계 결정:

- **정답은 스키마 필드만** (`assignments`, `reason`). 교사가 덧붙인 필드는 뺀다
- **추론은 선택** (`--include-reasoning`). 붙이면 `<think>…</think>` 뒤에 답. 잘리지 않은 호출의 추론만 쓴다
- **프롬프트·답이 같은 샘플은 실행이 달라도 한 번만**
- **`disagreements` 에 선호 라벨(chosen/rejected)을 붙이지 않는다.** 한 주기에 실제로 실행된 것은
  한쪽뿐이라 다른 쪽의 결과는 관측되지 않았다. 선호 데이터가 필요하면 같은 관측에서 양쪽을 각각
  실행하는 별도 실험을 해야 한다
- 옛 형식 로그(`raw/legacy/`)도 읽는다. 프롬프트는 관측으로 **현재 템플릿**을 써서 다시 만들고
  `prompt_rebuilt: true` 로 표시한다 — 당시 템플릿과 다를 수 있다

### 계속 쌓기 — 비교 실험을 반복한다

```bash
# Webots 에서 oneroom 을 열고 ▶ 한 뒤
src/webots_goal_bridge/scripts/run_explore_compare.sh mac 4          # 베이스라인·LLM 4쌍
python3 src/webots_goal_bridge/scripts/analyze_explore_compare.py    # 전략 비교
python3 src/webots_goal_bridge/scripts/export_distill.py             # 데이터셋 갱신
```

시행 이름표(`run_tag`)는 `{실험}_{묶음}_{커밋}_{전략}_{쌍}` 이다. 어떤 코드로 어떤 묶음에서 나온
데이터인지 파일만 보고 안다.

### 알아 둘 것

- **2026-09-16 이전 LLM 실행 로그는 없다.** 컨테이너 `/tmp` → 휘발성 임시 폴더로 옮기던 방식이라
  두 번 날아갔다. 그래서 기본을 영구 마운트로 바꿨다
- Ubuntu 네이티브 Docker 에서는 컨테이너가 root 로 써서 `data/distill/raw/` 파일이 root 소유가 된다.
  지우거나 옮길 때 `sudo` 가 필요할 수 있다
