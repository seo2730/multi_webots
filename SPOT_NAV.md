# Spot 자율주행 튜닝 기록

Spot의 Nav2 자율주행을 손보면서 겪은 것 전부 — **무엇을 바꿨고, 어디서 막혔고,
왜 그랬는지**. 값만 보려면 [nav2_spot.yaml](src/Webots-SummitXL/workspace/navigation/param/nav2_spot.yaml)
머리말에 표가 있고, 여기는 그 근거와 시행착오를 남긴다.

기체·드라이버 함수 설명은 [spot_driver_functions.md](spot_driver_functions.md),
드론 쪽 같은 작업은 [DRONE_NAV.md](DRONE_NAV.md)에 있다.

## 목차
- [1. 요약](#1-요약)
- [2. 적용한 변경](#2-적용한-변경)
- [3. 이슈와 해결 — 코드·설정](#3-이슈와-해결--코드설정)
- [4. 이슈와 해결 — 측정 방법론](#4-이슈와-해결--측정-방법론)
- [5. 미해결 / 다음 작업](#5-미해결--다음-작업)
- [6. 재측정하는 법](#6-재측정하는-법)
- [7. 추가 기록 — 컨테이너 재기동 순서](#7-추가-기록--컨테이너-재기동-순서-2026-08-16)

---

## 1. 요약

시작할 때 Spot은 **UGV와 같은 Nav2 파라미터**를 쓰고 있었고, 목표 도달률이 0~1/3에
배회(주행거리÷직선거리)가 2~5배였다. 세 층에서 문제가 있었다.

| 층 | 문제 | 상태 |
|---|---|---|
| Nav2 공통 | 리커버리가 통째로 죽어 있었다 (네임스페이스 누락) | ✅ 해결 |
| Spot 드라이버 | `cmd_vel`이 m/s가 아니라 **보폭 배율**이었다 | ✅ 해결 |
| Nav2 파라미터 | 속도·가속·footprint가 지상차량 값 | ✅ 해결 |
| 좌표계 | `slam_toolbox`가 **정답 odom에 보정을 얹어** 표류시켰다 | ✅ 해결 |
| 보행 속도 | 보폭만 만지고 **케이던스는 손도 안 댐** | ⚠️ 부분 (아래 5장) |
| 운용점 | 최고속을 그대로 써서 **회전 여유가 0** 이었다 | ✅ 해결 |
| 센서 | 스캔이 **자기 몸 끝을 못 보면서 자기 다리는 찍고 있었다** | ✅ 해결 |

속도는 시뮬 시각 기준으로 다시 재서 확정했다 — **최고 안정속 0.195 m/s,
제자리 회전 0.247 rad/s**. (처음 값들은 벽시계로 재서 최대 4배까지 틀렸다,
[4장 ①](#-속도를-벽시계로-쟀다))

**현재 성적 — 목표 3/3 도달, 오차 0.36~0.38 m, 표류 0.00 m, 넘어짐 없음.**
남은 것은 배회 1.9~2.2배다.

---

## 2. 적용한 변경

### 파일

| 파일 | 무엇 |
|---|---|
| `spot_driver.py` | `cmd_vel` 단위 환산, 보행 파라미터 런타임 노출, 보행 정지 버그 수정 |
| `navigation/param/nav2_spot.yaml` | **신규.** Spot 전용 Nav2 파라미터 (UGV·드론은 `nav2.yaml` 그대로) |
| `navigation/launch/nav2.launch.py` | `params_file`·`map_topic`·`cmd_vel_topic` 인자, behavior_server 프레임 수정 |
| `single_spot_launch.py` | `params_file`로 `nav2_spot.yaml` 지정 |
| `navigation/setup.py` | `nav2_spot.yaml` 설치 |

### 주요 값

| 항목 | UGV | Spot | 왜 |
|---|---|---|---|
| 컨트롤러 | DWB | **RPP** | DWB는 속도 샘플링이라 로봇 모델 오차에 흔들린다 |
| `footprint` | 0.7 × 0.5 | **1.1 × 0.5** | 몸통 실측. **폭은 건드리지 말 것** ([3장 ④](#-footprint-폭을-키웠더니-경로가-아예-안-나왔다)) |
| `inflation_radius` / `cost_scaling` | 0.6 / 3.0 | **0.85 / 2.0** | 스캔 사각 보완용. 사각을 없앤 지금은 과보정일 수 있다 |
| 스캔 `range_min` | 0.5 (다섯 개 공통) | **카메라별 0.20 / 0.45 / 0.70** | 머리는 낮추고 자기 몸이 보이는 쪽은 올린다 ([3장 ⑧](#-스캔이-자기-몸-끝을-못-본다)) |
| 리커버리 회전 | 1.0 rad/s | **0.24** | 낼 수 없는 속도를 목표로 삼으면 리커버리가 실패 |
| `desired_linear_vel` | (0.5) | **0.15** | 최고속 0.195 를 그대로 쓰면 회전 여유가 0 이라 간헐적으로 넘어진다 ([3장 ⑫](#-주행-중-간헐적으로-넘어진다--임계값이-아니라-확률이었다)) |
| `server_timeout` | 20 ms | **200 ms** | 부하가 높으면 액션 서버 응답이 20 ms를 넘긴다 ([3장 ⑨](#-follow_path-액션-서버-응답-시간-초과)) |
| `movement_time_allowance` | 10초 | **60초** | 제자리 회전은 "진전 없음"으로 잡힌다 ([5장](#-제자리-회전이-너무-느리다-배회의-진짜-원인)) |
| `map`→`odom` | slam_toolbox | **항등 정적 TF** | odom이 이미 정답값 ([3장 ⑩](#-mapodom-표류가-목표-오차로-그대로-나왔다)) |
| SLAM `transform_publish_period` | 0.05 | **0.0** | 위와 같음. SLAM은 지도만 만든다 |
| 보행 `swing_period` | — | **0.12** | 케이던스. 런타임 변경 가능 |
| 보행 `step_velocity` | — | **3.0** | 위와 같음 |

`spot_driver.py`의 환산 상수 (실측으로 맞춘 값):

| 상수 | 값 | 의미 |
|---|---|---|
| `SPEED_COEF` / `SPEED_EXP` | 1.371 / 0.81 | `v = 1.371 · L^0.81` — 보폭→속도는 **선형이 아니다** |
| `YAW_PER_RADPS` | 8.11 | `cmd_vel.angular.z`(rad/s) → 내부 YawRate 배율 |
| `MAX_YAW_RATE` | 2.0 | 0.5 → 2.0. 제자리 회전이 0.079 → **0.247 rad/s** ([5장](#-제자리-회전이-너무-느리다-배회의-진짜-원인)) |
| `MAX_STEP_LENGTH` | 0.090 | 이 이상은 걸음이 불안정해진다 |
| `MIN_STEP_LENGTH` | 0.015 | 이 아래는 보행이 안 걸린다 ([3장 ⑥](#-저속에서-보행이-아예-안-걸렸다)) |

---

## 3. 이슈와 해결 — 코드·설정

### ① 리커버리가 통째로 죽어 있었다

**증상** — 4 m 목표에 1.43 m 남기고 `ABORTED`. 로그:

```
[transformPoseInTargetFrame] target frame "odom" does not exist
[behavior_server] Initial checks failed for spin -> Aborting handle
[bt_navigator] Goal failed
```

**원인** — `nav2.yaml`에 `local_frame: odom`이라 적혀 있었는데, Humble의
`nav2_behaviors`에는 **그런 파라미터가 없다.** 실제 이름은 `global_frame`이고,
yaml에 없으니 Nav2 기본값(네임스페이스 없는 `odom`)이 그대로 쓰였다.

```
local_frame       = Parameter not set        <- 존재하지 않는 파라미터
global_frame      = odom                     <- 네임스페이스 누락
robot_base_frame  = ugv1/base_link           <- 이건 정상
```

**헷갈리는 지점** — **주행 자체는 멀쩡한데 리커버리만 죽는다.** 평소엔 잘 가다가
BT가 리커버리를 한 번이라도 타는 순간 목표가 통째로 실패한다.

**해결** — `nav2.launch.py`에서 behavior_server 노드에만 `{ns}/odom`으로 덮어쓴다.
(`global_frame` 치환값은 `{ns}/map`이라 이 노드엔 맞지 않는다. local_costmap과 같은 예외)

### ② 리커버리 회전 한계가 무시되고 있었다

**증상** — `Exceeded time allowance before reaching the Spin goal`이 반복.

**원인** — `spin:` 아래에 중첩해서 적었는데 Nav2는 **노드 레벨**에서 읽는다.
설정값이 무시되고 기본값 1.0이 쓰였다. 당시 Spot 최대가 0.08 rad/s 안팎이라 영원히 도달 못 한다.

```yaml
behavior_server:
  ros__parameters:
    max_rotational_vel: 0.24     # 여기 (노드 레벨) — 맞다
    spin:
      max_rotational_vel: 0.24   # 여기 — 조용히 무시된다
```

**해결** — 노드 레벨로 이동. ①과 **같은 부류의 함정**이다.

> ⚠️ `time_allowance`(기본 10초)는 BT XML 속성이라 yaml로 못 바꾼다. 회전 상한을
> 올린 지금은 10초에 141°를 돌 수 있어 여유가 생겼다 ([5장](#5-미해결--다음-작업)).

### ③ `cmd_vel`이 m/s가 아니었다

**증상** — 경로를 못 따라가고 배회가 직선거리의 2~5배.

**원인** — `StepLength = 0.15 × linear.x`, 즉 **보폭 배율**이었다. 그런데 Nav2의 DWB는
`linear.x`를 m/s로 알고 궤적을 예측한다. **컨트롤러의 세계 모형이 로봇과 어긋난 채로 돌았다.**

측정해 보니 회전이 특히 심했다 — **실제가 예측의 0.53배로 고정**. 상한을 어떻게 잡아도
이 비율은 안 바뀌므로 **파라미터로는 못 고친다.**

**해결** — 포화 지점에서 역산해 환산 계수를 넣었다.

```python
StepLength = (|v| / SPEED_COEF) ** (1/SPEED_EXP)   # v[m/s]
YawRate    = YAW_PER_RADPS * angular.z             # [rad/s]
```

수정 후 회전 오차 −47% → **+3~7%**. 목표 오차도 1.96 m → **0.45~0.50 m**(허용치 0.5 안).

### ④ footprint 폭을 키웠더니 경로가 아예 안 나왔다

**증상** — `GridBased: failed to create plan`. 전역 플래너가 경로를 못 만든다.

**원인** — Spot이 1.1 m로 길어서 footprint를 1.2 × **0.6**으로 키웠는데,
**NavFn은 내접 반경 안쪽을 통행 불가로 본다.**

| footprint | 내접 반경 | 장애물마다 막히는 폭 |
|---|---|---|
| 0.7 × 0.5 (UGV) | 0.25 | 0.25 m |
| 1.2 × **0.6** | **0.30** | 0.30 m |

가구 사이가 몸통 폭 정도면 양쪽에서 0.30씩 막혀 통과 가능 폭이 사라진다.

**해결** — **길이만 늘리고 폭은 UGV와 같게**(1.1 × 0.5). Spot의 다리는 주로 앞뒤로
스윙하지 좌우로 몸통보다 넓어지지 않는다.

### ⑤ 저속에서 33% 초과속

**원인** — 보폭↔속도가 선형이 아니다. 보폭이 작을수록 효율이 높다.

| StepLength | 0.015 | 0.030 | 0.050 |
|---|---|---|---|
| m/s ÷ L | 6.4 | 5.6 | 5.1 |

포화점에 맞춘 선형 계수 하나로는 저속을 못 맞춘다.

**해결** — 로그-로그 회귀로 지수를 맞춘 거듭제곱 환산. 저속 오차 33% → **2%**.

### ⑥ 저속에서 보행이 아예 안 걸렸다

**증상** — 명령 0.20 / 0.35 m/s에서 **이동 0.000 m, roll 정확히 0.0°**. 아예 안 움직인다.

**원인** — `Bezier.py`:

```
Tstance = 2L / StepVelocity
if Tstride < Tswing + dt:  RESET ALL     <- 보행이 진행 못 함
```

케이던스를 올리려고 `StepVelocity`를 3.0으로 **상수** 지정했더니, 보폭이 작을 때
`Tstance`가 dt(32 ms)보다 작아져 매 스텝 리셋됐다.

**해결** — 보폭에 맞춰 `StepVelocity`를 깎아 `Tstance ≥ 2·dt`를 보장한다.
큰 보폭에서는 원래 값을 그대로 쓰므로 최고 속도 손해가 없다.

### ⑦ 속도를 올리려면 보폭이 아니라 케이던스다

**증상** — 보폭만 키우니 0.40 m/s 상당에서 균형이 무너져(roll 4° → 17°) 더 못 올림.

**원인** — 속도를 정하는 것이 셋인데 하나만 만졌다.

```
Tstance = 2L / StepVelocity      Tstride = Tstance + SwingPeriod
속도    = 2L / Tstride
```

`SwingPeriod`(0.3)와 `StepVelocity`(0.8)는 **하드코딩된 채 손도 안 댔다.**

**해결** — 둘을 ROS 파라미터로 노출해 런타임 튜닝 가능하게 했다.
케이던스를 함께 올리자 같은 자세 안정성(roll 4°대)에서 **속도가 2.3배**가 됐다.

```bash
ros2 param set /spot1/spot_driver swing_period 0.15
ros2 param set /spot1/spot_driver step_velocity 2.0
```

### ⑧ 스캔이 자기 몸 끝을 못 본다

**증상** — 장애물에 접촉.

**원인** — 병합 스캔의 `range_min`이 **0.5 m**인데 footprint 반길이는 **0.55 m**다.
0.5 m 안쪽은 코스트맵에 아예 안 들어오고, 컨트롤러는 비어 있는 줄 알고 명령을 낸다.

**⚠️ 인플레이션만으로는 못 막는다.** 치명 판정은 footprint 내접 반경(0.25)이 정하고,
`inflation_radius`는 그 바깥의 비용 기울기일 뿐이다.

**완화** — 반경을 넓히고 감쇠를 늦춰 0.5 m 지점 비용을 올렸다.

```
cost = 253 * exp(-scaling * (거리 - 내접반경))
0.5 m 에서:   3.0/0.6 -> 119      2.0/0.85 -> 154
```

#### 해결 — `range_min`을 카메라마다 따로 준다

`range_min`을 그냥 낮추면 자기 다리를 장애물로 찍는다. 그래서 먼저 **어느 카메라가
자기 몸을 보는지** 쟀다. 로봇을 여유 2.00 m인 곳에 세워 두고(실제 최근접 장애물이
2.0 m이므로 그보다 가까운 반환은 전부 가짜) 카메라별로 본 결과:

| 카메라 | 가짜 반환 | 방위 | 정체 |
|---|---|---|---|
| 머리 좌/우 | **없음** (최소 1.94 m) | — | ✅ 깨끗 |
| 옆구리 좌/우 | 0.30 m | ±140° | 뒷다리 |
| 후방 | 0.54~0.61 m | ±168° | 몸 뒤쪽 |

**두 가지가 동시에 잘못돼 있었다.**

1. 후방의 0.54~0.61 m는 `range_min 0.5`를 **통과**했다. 즉 SLAM이 **자기 몸을
   지도에 적고 있었다.** 로봇 뒤 0.54 m에 늘 장애물 셀이 있던 것이 이것이다.
2. 머리 카메라는 근거리에서 깨끗한데도 0.5 m 안쪽을 버렸다. **진행 방향을 못 봐서**
   상자 옆 0.45 m에서 넘어졌다.

그래서 진행 방향은 낮추고, 자기 몸이 보이는 쪽은 올렸다.

```python
DEPTH_CAMERAS = {
    'left_head_depth':   0.20,   # 진행 방향 — 낮아야 충돌을 피한다
    'right_head_depth':  0.20,
    'left_flank_depth':  0.45,   # ±140도에 뒷다리
    'right_flank_depth': 0.45,
    'rear_depth':        0.70,   # ±168도에 몸 뒤쪽
}
```

> 🚨 **`multi_scan_merger`에도 자체 `range_min`(기본 0.3)이 있다.** 여기를 같이
> 낮추지 않으면 카메라별로 아무리 낮게 줘도 병합 단계에서 다시 잘린다 — 머리
> 카메라의 0.20이 통째로 무효가 된다. 값이 안 바뀌는 것 같으면 이걸 먼저 본다.

**결과** — 병합 스캔 최소가 1.94 m(= 실제 여유와 일치), 0.3~1.4 m 구간 반환 **0개**.
하한은 0.20 m까지 열렸다.

---

### ⑨ `follow_path` 액션 서버 응답 시간 초과

**증상** — 목표가 시작도 못 하고 죽는다.

```
Timed out while waiting for action server to acknowledge goal request for follow_path
```

**원인** — Nav2 BT 노드의 `server_timeout` 기본값이 **20 ms**다. 로봇 4대 + Webots를
한 PC에서 돌리면 CPU가 950%까지 올라가는데, 그 상태에서 컨트롤러 서버가
20 ms 안에 ack를 못 보낸다. **경로 계획이 실패한 게 아니라 응답이 늦은 것뿐이다.**

**해결** — `nav2_spot.yaml`의 `bt_navigator`에서 늘렸다.

```yaml
bt_navigator:
  ros__parameters:
    server_timeout: 200          # ms, 기본 20
    default_server_timeout: 200
```

> 💡 이 증상이 보이면 먼저 `docker stats`로 CPU를 본다. 부하가 원인이면
> 안 쓰는 로봇 컨테이너를 내리는 것도 같이 효과가 있다.

---

### ⑩ `map`→`odom` 표류가 목표 오차로 그대로 나왔다

**증상** — Nav2는 계속 `SUCCEEDED`를 반환하는데 실제로는 점점 엉뚱한 데 선다.
목표를 하나씩 돌 때마다 오차가 커진다.

```
목표                  거리    결과        오차     주행    배회   map->odom 표류
(  5.31, 23.77)      3.0m   SUCCEEDED   0.62m   4.53m   1.5x   1.10m
(  0.01, 25.70)      5.8m   SUCCEEDED   1.63m  13.88m   2.4x   2.83m
( -0.19, 28.10)      3.2m   시간초과      3.25m   0.67m   0.2x   2.81m
```

**오차와 표류가 거의 같은 값으로 같이 커진다.** 즉 Nav2는 자기 좌표계에서 제대로
도착하고 있고, **그 좌표계가 밀리고 있었다.**

**원인** — `slam_toolbox`가 `map`→`odom`을 발행하고 있었다.

이 프로젝트에서 **Spot의 odom은 추정값이 아니라 정답값이다.** `spot_driver.py`가
Supervisor로 자기 몸의 실제 위치를 읽어 그대로 발행한다(GPS 절대좌표). 그런데
`slam_toolbox`가 그 위에 스캔매칭 보정을 얹으면 **정답에 오차를 더하는 셈**이다.
게다가 Spot의 스캔은 뎁스카메라 5개를 합성한 것이라 라이다보다 노이즈가 커서
매칭이 더 잘 튄다.

수직으로도 밀려 있었다. `slam_toolbox`는 `map`→`odom`을
`(추정 pose) × (base→odom)`으로 만드는데, `base→odom`의 z(0.51 m)가 그대로 샌다.
실측 `map`→`odom` = `(-0.0725, -0.0118, -0.5063)`.

**해결** — SLAM은 **지도만** 만들게 하고, `map`→`odom`은 항등 정적 TF로 못 박았다.

```python
# single_spot_launch.py — [E] slam_toolbox
'transform_publish_period': 0.0,      # 0 = TF 발행 스레드를 아예 안 띄운다

# [E-2] 항등 변환 (LaunchDescription 에 넣는 것을 잊지 말 것)
Node(package='tf2_ros', executable='static_transform_publisher',
     namespace=ns,
     arguments=['0','0','0','0','0','0', f'{ns}/map', f'{ns}/odom'],
     parameters=[{'use_sim_time': True}])
```

> 🚨 **`publish_map_to_odom_tf`라는 파라미터는 Humble의 `slam_toolbox`에 없다.**
> 처음에 그 이름으로 `False`를 줬는데 **에러 없이 조용히 무시됐고**, 껐다고 착각한
> 채 표류를 계속 봤다. 확인법:
> ```bash
> strings /opt/ros/humble/lib/libtoolbox_common.so | grep -x transform_publish_period
> ```
> `ros2 param get`은 "Parameter not set"만 돌려줘서 구분이 안 된다.

확인 — 고친 뒤 `map`→`odom`은 정확히 `(0, 0, 0)`, `map`→`base_link`는 실제 월드
좌표와 z(0.51 m)까지 일치한다.

드론(`drone_layer_mapper`)이 이미 같은 이유로 SLAM 없이 항등 TF를 쓰고 있고,
맵 병합의 `odom_is_world_absolute: true`([MAP_MERGE.md](MAP_MERGE.md))와도 같은 전제다.

> ⚠️ **실기로 옮길 때는 되돌려야 한다.** 실제 로봇의 odom은 휠/IMU 적분이라
> 추정값이고, 그때는 SLAM 보정이 오차를 **줄인다.** 주기를 `0.05`로 되돌리고
> `[E-2]`를 지운 뒤 SLAM 파라미터를 다시 튜닝할 것.

---

## 4. 이슈와 해결 — 측정 방법론

**이 장이 실제로 제일 많은 시간을 잡아먹었다.** 코드가 아니라 재는 방법이 틀려서
결과를 여러 번 날렸다.

### 🚨 속도를 벽시계로 쟀다

**가장 큰 실수다.** 시뮬 속도를 **23% 고정**으로 가정하고 계산했다.

```python
act = 거리 / (벽시계시간 * 0.23)      # 틀렸다
```

그런데 시뮬 속도는 **컨테이너 수와 부하에 따라 23~90%로 변한다**(실측). 컨테이너 하나를
내리는 것만으로 4배가 바뀐다. **그래서 보고한 절대 속도값들이 최대 4배까지 틀렸다.**

**올바른 방법 — odom 헤더의 시뮬 시각을 쓴다.**

```python
def stamp():
    h = odom.header.stamp
    return h.sec + h.nanosec * 1e-9

dt = stamp_끝 - stamp_시작        # 벽시계가 아니다
v  = 이동거리 / dt
```

> 자세(roll/pitch) 기반 **안정성 판정과 항목 간 상대 비교는 유효하다** — 같은 조건에서
> 연속 측정했고, 각도는 시간 척도와 무관하기 때문이다.

### 테스트 노드에 `use_sim_time`을 안 켰다

**증상** — 목표는 수락되는데 **로봇이 한 발도 안 뗀다.**

```
Extrapolation Error: Requested time 2982.516 but the earliest data is at time 3364.256
```

목표 스탬프가 벽시계라 Nav2가 수백 초 과거로 TF를 조회한다. **이것 때문에 세 번을 헛돌았다.**

```python
n = Node('test', parameter_overrides=[Parameter('use_sim_time', value=True)])
```

> `ros2 topic pub`은 스탬프가 0이라 "최신"으로 처리돼 통과한다. 액션으로 스크립트를
> 짤 때만 문제가 된다.

### 목표를 아무 데나 찍었다

`현재위치 + (5, 3)` 같은 상대 좌표를 그대로 썼더니 **월드 밖**(x 최대 9.5인데 11.34)이나
벽 속을 찍었다. 그런 목표는 당연히 실패하는데 원인을 파라미터에서 찾느라 시간을 썼다.

**해결** — 목표를 맵에서 고른다.

- 주변 1.1 m에 **장애물 한 칸도 없을 것**(하드)
- 목표 자리 자체는 관측된 빈 곳, 주변 **55% 이상 관측**
  (100% 관측을 요구하면 후보가 0개가 된다 — `slam_toolbox` 맵은 지나온 자리 위주다)
- 로봇→목표 **직선 회랑(반폭 0.8 m)에 장애물 없음**
- **월드 경계 안**

### 디버깅한다고 `cmd_vel`을 오래 줬다가 월드 밖으로 밀어냈다

"물리적으로 낀 건지" 확인한다고 `linear.x`를 20초 직접 줬더니 Spot이 x=10.5(월드 밖)로
나가 벽 위에 올라앉았다. 그 뒤 **모든 목표가 실패**했고, 원인을 한참 지도에서 찾았다.

**짧게만 준다.** 그리고 측정 전에 항상 월드 안인지 확인한다.

### drone1을 내렸더니 시뮬 전체가 멈췄다

Spot만 테스트한다고 다른 로봇을 내렸는데 `/spot1/odom`이 0건으로 끊겼다.
**드론은 `synchronization TRUE`라 Webots가 매 스텝 드론 컨트롤러를 기다린다.**

부하를 줄이려면 **ugv2를 내리고 drone1·ugv1은 살려 둔다**(`ugv1`은 `/clock` 발행자).

### 로봇이 계속 장애물에 끼어 측정을 날렸다

이 세션에서 측정을 대여섯 번 날린 원인의 대부분이 "정면 0.54 m에 장애물" 또는
"월드 밖"이었다. 벽을 밀고 있으면 속도가 0으로 나와 **파라미터가 잘못된 것처럼 보인다.**

**측정 전에 반드시 확인한다.**

```python
front = min(정면 ±15° 스캔)     # 2.5 m 이상이어야 전진 측정이 유효
```

---

## 5. 미해결 / 다음 작업

### ~~절대 속도 재측정~~ ✅ 완료

시뮬 시각 기준으로 다시 쟀다. 최고 안정속 **0.195 m/s**, 최대 각속도
제자리 회전 **0.247 rad/s**. 환산 상수는 [2장 표](#주요-값)에 있다.
다시 잴 일이 있으면 조건은 그대로다:

- Spot을 **직진 5~6 m 가능한 트인 곳**에 놓을 것
- 측정 중 **컨테이너 구성을 바꾸지 말 것**(시뮬 속도가 변한다)
- 속도는 **반드시 `odom.header.stamp`** 로 나눌 것 ([4장 ①](#-속도를-벽시계로-쟀다))

### 리커버리 `time_allowance`가 Spot에게 짧다

`spin` 리커버리의 기본 `time_allowance`는 **10초**인데, Spot의 회전은 0.247 rad/s라
10초에 **1.1 rad(63°)** 밖에 못 돈다. 리커버리가 목표 각도에 못 닿고 시간 초과로
끝난다.

`time_allowance`는 YAML이 아니라 **BT XML의 `<Spin>` 노드 속성**이라 파라미터로는
못 바꾼다. 커스텀 BT XML을 만들어 `bt_navigator`의
`default_nav_to_pose_bt_xml`로 지정해야 한다. **아직 안 했다.**

### 🚨 제자리 회전이 너무 느리다 (배회의 진짜 원인)

`map`→`odom` 표류를 잡고 나니 다음 병목이 드러났다. 목표 오차는 0.62 → 0.38 m로
줄었는데 **배회가 5.0배**였고, 로그에 `Failed to make progress`가 **30초 주기로
정확히 반복**됐다.

기본 설정(`max_yaw_rate 0.5`)의 제자리 회전은 **0.079 rad/s** — 180° 도는 데 40초다.
`SimpleProgressChecker`는 이동량만 보므로 제자리 회전은 전부 "진전 없음"이고,
30초 허용치로는 회전 도중에 목표가 죽는다.

**완화** — `movement_time_allowance`를 30 → **60초**로 늘렸다. 증상 완화다.

**해결** — 회전 상한(`MAX_YAW_RATE`)을 0.5 → **2.0**으로 올렸다. 자세를 유지한 채
**3.1배** 빨라진다.

| `max_yaw_rate` | 실제 rad/s | 180° | 최대 roll | |
|---|---|---|---|---|
| 0.5 (원래 기본) | 0.079 | 40초 | 4.8° | |
| 0.8 | 0.124 | 25초 | 4.3° | |
| 1.2 | 0.192 | 16초 | 4.3° | |
| **2.0** | **0.247** | **13초** | 6.5° | ✅ 채택 |
| 2.5 | 0.256 | 12초 | **13.0°** | 더 안 빨라지고 자세만 무너진다 |

같이 바꾼 값:

| | 전 | 후 |
|---|---|---|
| `MAX_YAW_RATE` (드라이버) | 0.5 | **2.0** |
| `YAW_PER_RADPS` (드라이버) | 4.37 | **8.11** (= 2.0 ÷ 0.247) |
| `rotate_to_heading_angular_vel` (RPP) | 0.11 | **0.24** |
| 리커버리 `max_rotational_vel` / `min_` | 0.11 / 0.04 | **0.24 / 0.08** |

**회전 전용 보폭(`turn_step_length`)은 그대로 뒀다.** 0.015 / 0.035 / 0.060에서
0.232 / 0.227 / 0.198 rad/s로 거의 차이가 없다 — 회전 속도를 정하는 것은 보폭이
아니라 상한이다.

> 🚨 **이 표들은 소환 지점을 옮긴 뒤 다시 잰 것이다.** 옮기기 전 측정은 전부
> 쓰레기였고(아래 ⑪), 거기서는 "상한을 올리면 오히려 느려지고 2.0에서 뒤집힌다",
> "회전 보폭을 키우면 파국"이라는 **정반대 결론**이 나왔다. 물리 특성이 아니라
> **로봇이 상자에 걸린 것**이었다.

### 지금까지의 결과 (2026-08-16 기준)

| | 시작 | 표류 잡은 뒤 | 회전 상한 | 스캔·속도까지 |
|---|---|---|---|---|
| `map`→`odom` 표류 | 0.01 → **2.83 m** | 0.00 m | 0.00 m | **0.00 m** |
| 목표 오차 | 0.62 / 1.63 / 3.25 m | 0.38 m | 0.32 / 0.39 m | **0.36~0.38 m** |
| 배회 | 1.5 / 2.4x | 5.0x | 3.4 / 1.7x | **1.9 / 2.0 / 2.2x** |
| 도달률 | 2/3 | 1/2 | 2/3 | **3/3** ✅ |
| `Failed to make progress` | — | 30초마다 반복 | 안 나옴 | 안 나옴 |
| 넘어짐 | — | — | 접촉·보행 불안정 | **없음** |

마지막 열이 현재 설정이다 — 카메라별 스캔 하한([⑧](#-스캔이-자기-몸-끝을-못-본다)) +
`desired_linear_vel` 0.15([⑫](#-주행-중-간헐적으로-넘어진다--임계값이-아니라-확률이었다)).
**목표 3개 전부 도달했고, 오차가 0.36~0.38 m 로 고르고, 한 번도 넘어지지 않았다.**

측정 원본:

```
시작 (10.00, 22.77) | 맵 115x75 장애물 113 | 표류 0.00 m
( 11.66, 19.45)  3.7m SUCCEEDED   0.38m   8.20m  2.2x  0.00m
( 11.66, 25.15)  5.3m SUCCEEDED   0.36m  10.44m  2.0x  0.00m
(  9.16, 25.15)  2.4m SUCCEEDED   0.37m   4.74m  1.9x  0.00m
--- 성공 3/3
```

**남은 것은 배회 1.9~2.2배다.** 접촉·표류·넘어짐이 아니라 경로 추종 품질 문제이므로,
다음 시도는 인플레이션을 UGV 값(0.6 / 3.0)으로 되돌려 보는 것이다 —
스캔 사각을 메우려던 과보정이 좁은 통로를 비싸게 만들고 있을 수 있다.

### ⑫ 주행 중 간헐적으로 넘어진다 — 임계값이 아니라 확률이었다

**증상** — 스캔 사각(⑧)을 고친 뒤 Nav2 목표 주행 중에 로봇이 옆으로 넘어졌다
(`roll 90.9°`). 그 자리엔 **월드 파일상 장애물이 없다** — 접촉이 아니라 보행
불안정이다.

**틀린 가설 셋.** 회전 상한을 0.5 → 2.0으로 올린 직후라 그걸 의심했는데, 하나씩
시험해 보니 **전부 아니었다.**

| 의심한 것 | 시험 | 결과 |
|---|---|---|
| 회전 상한 2.0이 과하다 | 0.19 m/s + 0.24 rad/s 유지 | ❌ roll 6.4°로 통과 |
| 회전 방향 급반전 | 정지 없이 6회 부호 뒤집기 | ❌ roll 10.3°로 통과 |
| 특정 회전 속도가 임계 | 0.14 vs 0.24 rad/s | ❌ **더 약한 0.14가 넘어지고 0.24는 멀쩡** |

마지막 줄이 핵심이다. **단조롭지 않다 = 임계값이 아니다.** 같은 조건을 반복하면
통과와 넘어짐이 반반으로 나온다.

**원인** — 운용 속도가 한계에 붙어 있었다.

```
실측 최고 안정속           0.195 m/s   (직진만)
desired_linear_vel        0.19  m/s   <- 여유가 사실상 0
```

직진만 하면 견디지만 **회전이 얹히는 순간 넘어간다.** 회전 ±0.14 rad/s를 4초마다
급반전시키며 최대 roll을 재면:

| `desired_linear_vel` | 최대 roll | |
|---|---|---|
| 0.19 | 10.3° / 45.3° / **180°(뒤집힘)** | 같은 조건에서 결과가 갈린다 |
| **0.15** | **5.2°** | ✅ 채택 |
| 0.12 | 6.0° | 더 낮춰도 나아지지 않는다 |
| 0.10 | 5.6° | 〃 |

**해결** — `desired_linear_vel`을 0.19 → **0.15**로 내렸다. 0.15 아래로는 얻는 것이
없으므로 여기가 운용점이다.

> 💡 **교훈** — "최고 안정속"은 직진에서 잰 값이다. 그 값을 컨트롤러에 그대로 주면
> 회전·가감속에 쓸 여유가 남지 않는다. 운용점은 항상 한 단계 아래로 잡는다.

### ⑪ 소환 지점이 상자에서 0.25 m — 돌면 몸이 닿는다

측정이 4배씩 튀고 로봇이 반복해서 뒤집힌 이유다. **그 상태에서 얻은 결론은 전부
뒤집혔다** — 위 표의 옛 값들이 그렇다.

```
옛 소환 지점 (2.25, 23.75) 에서 crate_3 (2.5 x 2.5) 까지   0.25 m
Spot footprint 반길이                                       0.55 m
```

**제자리에서 돌면 몸통이 상자에 닿는다.** 같은 방에서 사방 여유가 가장 큰
**(5.50, 21.00)** (여유 2.00 m)으로 옮겼다 —
`config/fleet/oneroom.yaml`을 고치고 `gen_fleet_compose.py`로 compose 3벌을 재생성.

| 항목 | 옛 자리 (여유 0.25 m) | 새 자리 (여유 2.00 m) |
|---|---|---|
| 회전 @ `max_yaw_rate 0.5` | 0.029 rad/s, 재현 안 됨 | **0.079 rad/s, 재현됨** |
| 상한을 올리면 | 느려지고 뒤집힘 | 단조롭게 빨라짐 |

> 🚨 **스캔으로 여유를 재지 말 것.** 당시 새 자리에서도 스캔 최근접이 0.54 m로
> 나왔는데, 그건 벽이 아니라 **Spot이 자기 몸을 보는 것**이었다
> ([3장 ⑧](#-스캔이-자기-몸-끝을-못-본다)에서 고쳤다 — 지금은 1.94 m 로 실제 여유와
> 일치한다). 여유는 **월드 파일의 Solid 좌표·크기로** 계산하는 것이 확실하다.

### 실제 BD Spot 스펙(순항 1.3 m/s)은 현재 구조로 도달 불가

케이던스를 올릴수록 이론 대비 효율이 떨어진다(106% → 53%). 자세는 멀쩡한데 **발이
미끄러진다.** 원인은 균형이 아니라 **시간 해상도**다 — 월드에 `basicTimeStep`이 없어
Webots 기본값 32 ms로 도는데, `swing=0.10`이면 스윙 한 번이 **3 스텝**뿐이다.

| 선택지 | 기대 | 대가 |
|---|---|---|
| 현행 유지 | 케이던스 튜닝분(약 2.3배) | 없음 |
| `basicTimeStep` 16 | 더 높은 케이던스 | 시뮬 **2배 느려짐** |
| `basicTimeStep` 8 | 1 m/s대 도전 | 시뮬 **4배 느려짐** |

**월드 전체(드론·UGV 포함)에 영향을 주는 결정**이라 임의로 바꾸지 않았다.

### ~~스캔 사각 (0.5 m)~~ ✅ 해결

카메라별 `range_min` 으로 고쳤다 ([3장 ⑧](#-스캔이-자기-몸-끝을-못-본다)).
진행 방향은 0.20 m 까지 본다.

**딸려 나온 숙제** — 인플레이션(0.85 / 2.0)은 그 사각을 비용으로 메우려던 보정이었다.
사각이 없어졌으니 UGV 값(0.6 / 3.0)으로 되돌려 보는 것이 다음 시도다. 아직 안 했다.

### 배회 1~2배

DWB→RPP로 2~5배에서 줄었지만 여전히 직선거리의 1~2배다. RPP의 `lookahead_dist`
튜닝 여지가 남아 있다.

---

## 6. 재측정하는 법

절차와 스크립트는 [spot_driver_functions.md](spot_driver_functions.md#자율주행-직접-테스트하기)에
있다. 순서만 요약하면:

1. **Spot을 트인 곳에 놓는다** — 정면 여유 2.5 m 이상 확인
2. **컨테이너 구성을 고정한다** — 측정 중 켜고 끄지 않는다
3. `use_sim_time=True`로 테스트 노드를 만든다
4. **시뮬 시각(odom stamp)으로** 구간을 잰다
5. 자세(roll/pitch)와 몸높이(z)를 함께 기록한다 — 안정성 판정용

```bash
# 보행 파라미터는 런타임에 바꿀 수 있다 (재빌드 불필요)
ros2 param set /spot1/spot_driver swing_period 0.15
ros2 param set /spot1/spot_driver step_velocity 2.0
ros2 param set /spot1/spot_driver max_step_length 0.07
```

> ⚠️ 노드가 100개를 넘으면 `ros2 param set` CLI가 20초 타임아웃으로 죽는다.
> 그때는 rclpy에서 `rcl_interfaces/srv/SetParameters`를 직접 부른다.

### Nav2 목표 시험을 돌리기 전에

1. **`map`→`odom`이 (0,0,0)인지 먼저 본다.** 아니면 뭔가 이 링크를 발행하고
   있는 것이고, 그 상태의 목표 오차는 전부 무의미하다 ([3장 ⑩](#-mapodom-표류가-목표-오차로-그대로-나왔다)).
2. **맵이 찼는지 본다.** 컨테이너를 새로 띄우면 SLAM이 처음부터 시작해서
   로봇 주변 몇 m만 있다. 이 상태에서 목표를 고르면 후보가 하나도 안 나온다.
3. 목표는 **각도 스윕이 아니라 관측된 빈 셀 전수 검색**으로 고른다.
   맵의 대부분이 미탐색이라 정해진 각도/거리에서는 안 걸린다.
4. 회랑(로봇→목표 직선)이 비었는지 볼 때 **로봇 코앞 1 m는 건너뛴다.**
   [3장 ⑧](#-스캔이-자기-몸-끝을-못-본다) 때문에 로봇 반경 0.5~0.8 m에 장애물 셀이
   찍혀 있어서, 거기부터 재면 **744개 후보가 전부 탈락**한다(실측). 그 구간은
   Nav2 지역 코스트맵이 담당한다.

---

### 관련 문서

- [spot_driver_functions.md](spot_driver_functions.md) — 드라이버 함수와 `cmd_vel` 단위
- [nav2_spot.yaml](src/Webots-SummitXL/workspace/navigation/param/nav2_spot.yaml) — 값과 근거표
- [DRONE_NAV.md](DRONE_NAV.md) — 드론 쪽 같은 작업
- [INTERFACES.md](INTERFACES.md) — 토픽·프레임 규격

---

## 7. 추가 기록 — 컨테이너 재기동 순서 (2026-08-16)

### 개별 컨테이너만 재기동하면 드라이버가 죽은 몸에 붙는다

**증상** — `spot1`은 정상 실행 중이고 로그에 `Controller successfully connected`까지
찍히는데 **`/spot1/odom`이 0건**이고 `spot1` TF가 하나도 안 나온다. Nav2는 이렇게 운다.

```
Could not find a connection between 'spot1/map' and 'spot1/base_link'
because they are not part of the same tree. Tf has two or more unconnected trees.
```

**진단의 결정적 단서** — 센서 토픽(`/spot1/front_left_dist` 등)은 **정상으로 나온다.**

| | 발행 주체 | 상태 |
|---|---|---|
| `front_left_dist` 등 | `webots_ros2_driver` **코어** (URDF `<device>` 자동) | ✅ 나옴 |
| `odom`, TF | `spot_driver.py` **플러그인** `step()` | ❌ 안 나옴 |

즉 **드라이버 코어는 Webots와 통신하는데 플러그인만 안 돈다.** 예외도 Traceback도 없이
조용히 멈춘다.

**원인** — `spot_driver`는 `getFromDef("SPOT1")`으로 자기 몸 노드를 잡아 매 스텝
위치를 읽는다. 소환기가 몸을 **제거하고 다시 넣으면 그 참조가 죽는다.**

이 순서가 깨지는 경우:

- 로봇 컨테이너만 `up -d --force-recreate {robot}` 로 재기동
- `fleet` 만 재기동 (소환기가 "뇌 없는 몸"을 지우고 새로 넣는다)
- 위 둘을 섞어서 반복

**해결 — 전체를 함께 세운다.**

```bash
docker compose -f docker-configs/windows/docker-compose.yml down
docker compose -f docker-configs/windows/docker-compose.yml up -d
```

`down` → `up` 이면 소환기가 몸을 다 넣고 `/tmp/fleet_ready` 를 만든 뒤에야 로봇
컨테이너가 풀리므로, 드라이버가 **처음부터 올바른 몸**에 붙는다.
(`depends_on: fleet: condition: service_healthy` 가 그 순서를 보장한다)

> Webots 자체는 `down` 으로 안 꺼진다. 로봇 몸만 지워졌다 다시 들어온다.

### 뒤집힌 Spot 되살리기

`roll 180°`, `z ≈ 0.13` 이면 뒤집힌 것이다. `/spawn_robot` 서비스는 `force: true` 를
줘도 **이름 충돌로 거부**한다(`이미 'spot1'라는 로봇이 월드에 있습니다`).

```bash
# 뇌를 내리면 소환기가 "뇌 없는 몸"으로 보고 제거 후 재소환한다
docker compose ... stop spot1
docker compose ... up -d --force-recreate fleet
docker compose ... up -d spot1
```

다만 이 방법은 위의 순서 문제를 일으킨다. **결국 `down` → `up` 이 가장 확실하다.**

### 남은 `docker run --rm` 컨테이너가 CPU를 먹는다

`docker run --rm ... windows-master python3 ...` 가 끝나지 않고 남는 경우가 있다
(`suspicious_shirley` 같은 임의 이름). 시뮬 속도가 이유 없이 느려지면 확인한다.

```bash
docker ps --format "{{.Names}}\t{{.Image}}" | grep -v _brain_ | grep -v fleet_
```

### 소환 지점이 장애물 옆이면 경고가 뜬다

```
[spot1] 맵 검사 실패(반경 0.90m 안에 장애물이 있습니다)지만 그대로 진행합니다
```

`arena_s3` 의 `(7.25, 27.75)` 가 그렇다. **속도 측정에는 부적합**하므로 매니페스트
좌표를 트인 곳으로 옮기거나, 측정 전에 트인 방향으로 돌려야 한다.
