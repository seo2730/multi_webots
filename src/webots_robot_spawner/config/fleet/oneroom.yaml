# oneroom 월드용 편대 — gen_world_random.py 가 함께 생성했다 (seed 1).
#
# 좌표는 격자에서 벽·장애물과 떨어진 셀 중 사분면별로 하나씩 골랐다.
# 지형이 시드마다 다르므로 손으로 고르면 벽 속에 로봇을 놓기 쉽다.

fleet:
  - {type: ugv, id: ugv1, x: -47.75, y: -23.25, yaw: 0.0}
  - {type: ugv, id: ugv2, x: 43.75, y: -23.75, yaw: 0.0}
  # 🚨 spot1 은 (2.25, 23.75) 였는데 crate_3 (2.5x2.5) 가 **0.25 m** 앞이었다.
  #    Spot 은 몸통 반길이가 0.55 m 라 그 자리에서는 **제자리에서 돌기만 해도 상자에
  #    닿는다.** 회전 실측이 같은 설정에서 0.029~0.123 rad/s 로 4배씩 튀었고 로봇이
  #    반복해서 뒤집혔다 (07_SPOT_NAV.md 5장).
  #    같은 방에서 사방 여유가 가장 큰 (5.50, 21.00) 으로 옮겼다 — 여유 2.00 m.
  - {type: spot, id: spot1, x: 5.50, y: 21.00, yaw: 0.0}
  - {type: drone, id: drone1, x: -46.25, y: 23.75, yaw: 0.0}

spawn_area: [-48.0, -48.0, 48.0, 48.0]
